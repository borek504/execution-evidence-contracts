name: tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  unittest:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Run focused unit tests
        run: python -B -m unittest -v test_r14_execution_contract
      - name: Run demo regression tests
        run: python -B -m unittest -v test_demo_execution_contract
      - name: Run Quick Start demo
        run: python -B demo_execution_contract.py
